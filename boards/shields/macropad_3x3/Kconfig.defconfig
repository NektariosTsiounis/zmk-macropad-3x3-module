# Macropad Configuration
if SHIELD_MACROPAD_3X3

config ZMK_KEYBOARD_NAME
    default "Macropad"

endif # SHIELD_MACROPAD_3X3


# Display Configuration
if ZMK_DISPLAY

config I2C
    default y

config SSD1306
    default y

endif # ZMK_DISPLAY


# LVGL (LittlevGL Graphics Library) Configuration
if LVGL

config LV_Z_VDB_SIZE
    default 64

config LV_DPI_DEF
    default 148

config LV_Z_BITS_PER_PIXEL
    default 1

choice LV_COLOR_DEPTH
    default LV_COLOR_DEPTH_1
endchoice

endif # LVGL
